# Environment Configuration
# Copy this file to .env and fill in your values

# OpenAI API Key (optional but recommended for better module extraction)
OPENAI_API_KEY=your-openai-api-key-here
# Default crawling settings (can be overridden in UI)
MAX_PAGES=50
MAX_DEPTH=4
CRAWL_DELAY=0.5
